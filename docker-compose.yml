services:
  AWRelay:
    image: awdress/awrelay:latest # Docker Hub 镜像，如非本人请替换为对应用户名
    container_name: AWRelay
    restart: unless-stopped
    # 从 .env 文件读取 BOT_TOKEN / ADMIN_ID / PROXY_URL，避免在此明文暴露 Token
    env_file:
      - .env
    volumes:
      - ./data:/app/data
